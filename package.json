{
  "name": "neportal",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "db:generate": "dotenv -e .env -- pnpm --filter @neportal/database exec prisma generate",
    "db:push": "dotenv -e .env -- pnpm --filter @neportal/database exec prisma db push",
    "db:migrate": "dotenv -e .env -- pnpm --filter @neportal/database exec prisma migrate dev",
    "db:seed": "dotenv -e .env -- pnpm --filter @neportal/database exec prisma db seed",
    "db:studio": "dotenv -e .env -- pnpm --filter @neportal/database exec prisma studio"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=20"
  }
}
